<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KhadamatiApp"
        tools:targetApi="31">

        <!-- 1. شاشة البداية (تفتح أولاً) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. شاشة تسجيل الدخول -->
        <activity android:name=".LoginActivity" android:exported="false" />

        <!-- 3. شاشة إنشاء حساب جديد -->
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- 4. الشاشة الرئيسية (قائمة الخدمات) -->
        <activity android:name=".MainActivity" android:exported="false" />

        <!-- 5. شاشة لوحة تحكم الأدمن -->
        <activity android:name=".AdminDashboardActivity" android:exported="false" />

        <!-- 6. شاشة تفاصيل الخدمة -->
        <activity android:name=".DetailActivity" android:exported="false" />

        <!-- 7. شاشة نموذج طلب الخدمة -->
        <activity android:name=".RequestServiceActivity" android:exported="false" />

    </application>

</manifest>